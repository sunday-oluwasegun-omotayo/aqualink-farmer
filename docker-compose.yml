version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: aqualink_farmer
      POSTGRES_USER: aqualink
      POSTGRES_PASSWORD: aqualink_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: